# Copy this file to .env and fill values (do NOT commit your real keys)
# SendGrid API key for sending emails
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Destination email address that will receive the contact messages
TO_EMAIL=your.email@example.com

# Optional: the from address that will appear in sent emails (should be a verified sender in SendGrid)
FROM_EMAIL=no-reply@yourdomain.com
